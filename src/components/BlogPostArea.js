import React, { Component } from 'react';
import '../css/app.css';

class BlogPostsList extends Component {
    render() {
        return (
            <div>This is the area where the current selected blog post is displayed.</div>
        );
    }
}

export default BlogPostsList;