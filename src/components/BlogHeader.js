import React, { Component } from 'react';
import '../css/app.css';

class BlogHeader extends Component {
    render() {
        return (
            <div>This is the simple blog's header.</div>
        );
    }
}

export default BlogHeader;